# etch_ui 구현 상태 (코드 기준)

> **갱신:** 2026-06-04 · 커밋 `9ede039` 이후  
> **저장소:** [github.com/yaong832/etch_ui](https://github.com/yaong832/etch_ui)  
> **Flask:** 별도 `etchflask` (`C:\etchflask` 로컬 예시)

---

## Phase 요약

| Phase | 내용 | 상태 |
|:-----:|------|:----:|
| **0** | PLC·HMI·Flask 클라이언트 기반 | ✅ |
| **1** | WPF 단독 마감 (AlarmCatalog, 연결 UX, FOUP, Hmi/* 분리) | ✅ `a16b275` |
| **2** | Flask payload 계약·연결 UX (`EtchTelemetryContractValidator`) | ✅ `9ede039` |
| **3** | AI JSONL 헤드리스 검증 · AlarmCatalog↔AI 조언 | ✅ |
| **4** | TwinCAT 현장 · `train_from_sim -Deploy` 수동 E2E | 🔜 |
| **5** | MainWindow 추가 분리 · 2창 UI (가상/실장) | 보류 |

---

## WPF 화면 (현재 `MainWindow.xaml`)

| 영역 | 비율 | 성격 |
|------|------|------|
| **좌** | 7.5* | 가상 TM 도식 · TM 진단 분리 창 |
| **중** | 2* | 인터락·공정 스텝·AI 요약 |
| **우** | 1.6* | 램프·Start/Stop · 잔량 타일 |
| **하단** | 로그 스트립 | 운영 로그 |

실측 센서는 **↗ 센서** 분리 창. EtherCAT·Load Lock 접촉은 인터락·헤더 칩.

---

## `Services/Hmi/` (Flask·연결)

| 클래스 | 역할 |
|--------|------|
| `HmiConnectionPresenter` | EtherCAT·데이터 품질 칩 문구 |
| `HmiFlaskStatusPresenter` | Flask OK/OFF · 마지막 성공 시각 |
| `HmiTelemetryPublisher` | POST + 로컬 SQLite |
| `HmiFlaskGateway` | 헬스·이벤트·AI GET |
| `HmiTelemetryPayloadFactory` | sensor-data 조립 |
| `EtchTelemetryContractValidator` | live/demo/offline JSON 계약 |

---

## 헤드리스 검증 (Release)

```bash
dotnet run -c Release -- --sim-smoke --ticks=4000
dotnet run -c Release -- --sim-aligner-audit --ticks=160000
dotnet run -c Release -- --sim-stress --ticks=200000
dotnet run -c Release -- --sim-ai-jsonl --ticks=120
```

---

## AI 파이프라인

1. RUNNING 중 `data/ai_training_snapshots.jsonl` (1Hz)
2. `tools/ai/train_from_sim.ps1 -Deploy`
3. Flask `GET /api/etch/ai/status` → `ready: true`
4. WPF `GET /api/etch/ai/latest` → 인터락 패널 (조언만)

상세: [`tools/ai/README.md`](../tools/ai/README.md) · [`AI_학습_모델_경로.md`](AI_학습_모델_경로.md)

---

## 미완 (우선순위)

1. Flask 수동 E2E (`PROTO_실행순서.md` B1~B8)
2. `train_from_sim.ps1 -Deploy` 후 ML stub=false 확인
3. TwinCAT 현장 (장비)
4. `MainWindow` → `PlcPollingService` / `InterlockEvaluator`
5. 가상·실장 2창 UI (검토 보류)

체크리스트: [`TODO.md`](TODO.md)
