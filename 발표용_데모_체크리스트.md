# 발표용 데모 체크리스트

PLC·TwinCAT 없이 **발표 PC**에서 3~5분 시연 + 사전·사후 검증용입니다.  
상세 실행·API: [`PROTO_실행순서.md`](PROTO_실행순서.md)

---

## 사전 준비 (발표 10분 전)

| # | 항목 | 확인 |
|---|------|:----:|
| P1 | `C:\etchflask\run_flask.bat` 실행 → 브라우저 `http://127.0.0.1:5000` 열기 | ☐ |
| P2 | WPF `etch_ui.sln` 빌드 성공 (`dotnet build -c Release`) | ☐ |
| P3 | `appsettings.json` → `FlaskBaseUrl` = `http://127.0.0.1:5000` | ☐ |
| P4 | (선택) `SimulationEnabled: true` — 없어도 로그인 후 **시뮬 허용**으로 동일 | ☐ |
| P5 | 발표 계정: `admin` / `Admin1234` (또는 작업자 이상) | ☐ |
| P6 | 헤드리스 스모크 (아래 **자동 검증** 명령) 1회 통과 | ☐ |
| P7 | (2모니터·프로젝터) **도식 확대** → **→ 외부 모니터** · 위치 저장 확인 | ☐ |

**처음 쓰는 분:** [`docs/HMI_초보자_가이드.md`](docs/HMI_초보자_가이드.md) · [`docs/FLASK_초보자_가이드.md`](docs/FLASK_초보자_가이드.md)

---

## 3분 발표 스크립트 (권장 순서)

| 시간 | 화면 | 조작 | 관객에게 보여 줄 것 |
|------|------|------|---------------------|
| 0:00 | WPF 로그인 | `admin` 로그인 | HMI 타이틀·사용자 칩 |
| 0:20 | 헤더 | **시뮬 허용** ON | EtherCAT **DEMO (시뮬)** · 데이터 **데모 시뮬** |
| 0:40 | (선택) | **데모 진행** 클릭 | 하단 로그에 1/5~5/5 자동 안내 (~10초) |
| 1:00 | 우측 | **▶ 시작** (또는 데모 진행이 Start까지 수행) | 상태 **RUNNING** · 램프 녹색 계열 |
| 1:20 | 좌측 도식 | **도식 확대** (2모니터면 **→ 외부 모니터**) | TM·도어·FOUP ProgressBar·모듈 색 변화 |
| 1:40 | 중앙 | (관찰) | 공정 스텝 래더·인터락 **[데모]** 표기 |
| 2:00 | 중앙 하단 | (관찰) | **AI 조언** — Flask 연결 시 문구 갱신 |
| 2:20 | 헤더 | **대시보드** | Flask 실시간 · **모듈 상태** · **레시피** · **AI 진단** 탭 |
| 2:35 | (관리자) | **Maint** → **정비 도구** | FOUP/BM/PM 조작 · 이송 1틱 (시뮬 허용) |
| 2:50 | 우측 | **■ 정지** | 이송 **일시정지** (상태 유지) · **Start**로 재개 |
| 3:00 | 마무리 | 한 줄 멘트 | “실장비는 EtherCAT·Load Lock 접촉 인터락, AI는 조언만” |

**멘트 예시 (한 줄)**  
> “지금은 시뮬 데모입니다. 실측·접촉 인터락은 현장 PLC에서만 적용되고, 웹·AI는 모니터링·조언용입니다.”

---

## 발표 후·사전 전체 검증 (체크리스트)

### A. Flask · 웹

| # | 확인 내용 | 기대 |
|---|-----------|------|
| A1 | `GET /api/sensors` | HTTP 200 (`dataSource`는 WPF 미연결 시 `offline` 가능) |
| A2 | WPF RUNNING 후 `GET /api/etch/modules/latest?source=demo` | 모듈 배열 응답 |
| A3 | `GET /api/etch/recipe/active?source=demo` | 레시피 JSON |
| A4 | `GET /api/etch/ai/status` · `.../ai/latest` | HTTP 200 |
| A5 | 웹 **실시간** 탭 KPI·차트 갱신 | WPF 텔레메트리 (~2초) 후 변화 |

### B. WPF HMI (수동 — 발표 PC에서 1회)

| # | 확인 내용 | 기대 |
|---|-----------|------|
| B1 | 로그인 후 **시뮬 허용: 켬** | `PlcStatusText` = DEMO (시뮬) |
| B2 | **시작** 가능 (유지보수 OFF) | `CanStart` 활성 |
| B3 | RUNNING 30초+ | 도식 이송·FOUP 잔량·공정 스텝 변화 |
| B4 | **정지** → **시작** | 일시정지 후 FOUP·BM 상태 유지 · Start로 **재개** (LOT 완료 후 Start는 **새 LOT**) |
| B8 | (관리자) **Maint** → 정비 도구 | 슬롯 비우기·FOUP 재장착·1틱 이송 |
| B5 | **이벤트 로그** · **알람 이력** 버튼 | 창 열림·SQLite 조회 |
| B6 | (관리자) **설정** 저장 | appsettings 반영 로그 |
| B7 | **데모 가이드** · **데모 진행** | 안내 창·하단 로그 5단계 |

### C. 헤드리스 (터미널 — CI·발표 전날)

```powershell
cd D:\WPFProject\etch_ui
dotnet build -c Release
dotnet run -c Release --no-build -- --sim-smoke --ticks=5000
dotnet run -c Release --no-build -- --sim-policy-batch --runs=3 --ticks=3000
dotnet run -c Release --no-build -- --sim-maintenance --ticks=800
dotnet run -c Release --no-build -- --sim-dual-blade --ticks=12000
```

| # | 명령 | 기대 |
|---|------|------|
| C1 | `--sim-smoke` | `success=True` |
| C2 | `--sim-policy-batch` | `success=True` |
| C3 | `--sim-maintenance` | `success=True` (일시정지·재개·정비 API) |
| C4 | `--sim-dual-blade` | `success=True` |

(장시간 회귀: `--sim-report --ticks=250000` — 발표 직전 필수 아님)

### D. 실장비 전용 (발표에서 **생략** 가능)

| # | 항목 | 비고 |
|---|------|------|
| D1 | EtherCAT Connected + 실측 센서 | 현장 PC |
| D2 | 접촉 닫힘 → Start / 접촉 열림 → A004 | `PROTO_실행순서.md` Phase 1.2 |
| D3 | DI0~3 HW 버튼 | UI와 동일 동작 |

---

## 자동 검증 기록

에이전트·스크립트로 재현 가능한 항목만 아래에 적습니다. **B. WPF 수동**은 발표 PC에서 체크박스를 직접 채웁니다.

| 일시 | 항목 | 결과 |
|------|------|------|
| 2026-06-05 | `dotnet build -c Release` | **PASS** (경고 0) |
| 2026-06-05 | `--sim-smoke --ticks=5000` | **PASS** `max_side_storage=25` |
| 2026-06-05 | `--sim-policy-batch --runs=3 --ticks=3000` | **PASS** |
| 2026-06-05 | Flask 기동 `run_flask.bat` | **PASS** |
| 2026-06-05 | `GET /api/sensors` | **PASS** (POST 후 `demo`) |
| 2026-06-05 | `modules/recipe/ai` demo·status API | **PASS** (4종) |
| 2026-06-05 | `--sim-flask-e2e --require-ml` | **PASS** `engine=sklearn` |
| 2026-06-05 | `.\tools\flask\e2e_flask.ps1` | **PASS** (A1~A5 자동) |
| 2026-06-04 | `--sim-maintenance --ticks=800` | **PASS** (일시정지·재개·정비 API) |
| 2026-06-04 | `--sim-dual-blade --ticks=12000` | **PASS** |
| 2026-06-04 | `--sim-efem-audit --ticks=40000` | **PASS** `lot=75/75` |
| 2026-06-04 | `--sim-smoke` · `--sim-policy-batch` (재검증) | **PASS** |
| 2026-06-05 | WPF UI 수동 (B1~B8) | **미실행** — F5 후 위 B표 체크 권장 |

재검증:

```powershell
cd D:\WPFProject\etch_ui
dotnet build -c Release
dotnet run -c Release --no-build -- --sim-smoke --ticks=5000
dotnet run -c Release --no-build -- --sim-flask-e2e --require-ml
.\tools\flask\e2e_flask.ps1 -RequireMl
Invoke-RestMethod http://127.0.0.1:5000/api/etch/ai/status
```

---

## 문제 발생 시 빠른 대응

| 증상 | 조치 |
|------|------|
| Flask 칩 **OFF** | `run_flask.bat` 재실행 · 방화벽 5000 · URL 확인 |
| **시작** 비활성 | **시뮬 허용** ON · 유지보수 배너 해제 · 작업자 이상 로그인 |
| 도식 안 움직임 | RUNNING 여부 · 시뮬 허용 · 로그에 Start 거부 메시지 확인 |
| AI 패널 비어 있음 | Flask 실행 · WPF Flask 칩 **OK** · 6초+ 대기 |
| EtherCAT 실패 알람 | 발표 모드: **시뮬 허용** ON → DEMO 전환 (정상) |

---

## 관련 버튼·문서

| UI | 코드·문서 |
|----|-----------|
| 시뮬 허용 / 데모 진행 | `MainWindow.xaml` · `DemoScenarioRunner.cs` |
| 3분 창 | `DemoGuideWindow.xaml` |
| 스케줄·FOUP 정책 | [`SCHEDULER_FOUP_PM_정책.md`](SCHEDULER_FOUP_PM_정책.md) |
| AI 학습 수집 | `tools/ai/train_from_sim.ps1` · `docs/AI_학습_모델_경로.md` (있을 경우) |
